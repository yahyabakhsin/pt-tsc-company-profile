# Static Assets (`src/assets/`)

Local static files used in the application.

- `images/` - Local mockups, diagrams, and project previews.
- `icons/` - SVG icons if custom icons are needed beyond Lucide React.
- `documents/` - Static PDF datasheets or files served directly.
