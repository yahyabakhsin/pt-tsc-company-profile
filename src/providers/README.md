# Context Providers (`src/providers/`)

State providers, Theme providers, and QueryClients that wrap the application layout.

## Included Providers:
- `query-provider.tsx` - TanStack React Query Client wrapper.
- `theme-provider.tsx` - Dark-theme system provider.
- `toast-provider.tsx` - Sonner notifications configuration.
