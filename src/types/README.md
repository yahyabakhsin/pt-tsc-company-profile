# TypeScript Declarations (`src/types/`)

Contains global TypeScript type aliases, namespaces, interfaces, and module overrides.

## Structure:
- `index.ts` - Main export entry for shared types.
- `models.ts` - Database-equivalent models if distinct from Prisma output.
- `api.ts` - Common API Response structures.
