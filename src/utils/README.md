# Utility Functions (`src/utils/`)

Pure helper functions. Utilities are non-UI and must remain stateless.

## Files:
- `cn.ts` - Merging Tailwind CSS classes (clsx + tailwind-merge).
- `format.ts` - Date, currency, size, and number formatting.
- `logger.ts` - Structured server and client logger.
