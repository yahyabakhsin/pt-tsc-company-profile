# Global Styles (`src/styles/`)

Global CSS themes, font definitions, and responsive overrides.

Note: The main application entry imports `@import "tailwindcss";` in `src/app/globals.css`. Custom utility CSS files can be placed here and imported.
