# Application Constants (`src/constants/`)

Place immutable configuration variables and constant lists here.

## Recommended Files:
- `navigation.ts` - Footer link arrays and header navbar links.
- `metadata.ts` - Static metadata configurations for fallback SEO.
- `industries.ts` - Predefined lists of target sectors (e.g. food production, automation).
