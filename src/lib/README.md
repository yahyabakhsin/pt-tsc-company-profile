# Library Initializations (`src/lib/`)

Wrappers, initializations, and clients for third-party libraries.

## Examples:
- `axios.ts` - Configured Axios instances with default timeout and base API routing headers.
- `framer.ts` - Shared Framer Motion transitions and animation configurations.
